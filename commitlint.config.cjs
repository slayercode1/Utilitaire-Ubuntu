/**
 * Messages de commit au format Conventional Commits, comme l'historique
 * existant (feat, fix, docs, chore, refactor, test, …).
 */
module.exports = {
  extends: ['@commitlint/config-conventional']
}
